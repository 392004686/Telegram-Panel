# 问题修复清单

## 客户查询模块

### 1. 客户列表 - 批量操作栏溢出、对齐问题
**问题**: 批量操作栏在最大行时会跟导航列跑，导致操作行不跟客户列表
**修复**: 修改 CSS，使用正确的 sticky 定位和容器宽度控制

### 2. 姓名、昵称字段
**问题**: 姓名和昵称是否只会有一个？如果只有一个就删除其中之一
**状态**: 需要确认业务逻辑后处理

### 3. 活跃状态列显示详细时间
**问题**: 活跃状态列需要显示具体时间
**当前**: `activity(row)` 函数返回状态标签和可选时间
**修复**: 已经有 `lastSeenAt` 字段显示，需要优化显示格式

### 4. 查询手机号不返回账号名称问题
**问题**: 查询手机号实际不会返回账号名称，查询账号可能就会返回昵称
**修复**: 需要检查后端查询逻辑和前端显示逻辑

### 5. 历史查询筛选未完成
**问题**: 历史查询"筛选：尚未完成"
**修复**: 需要实现历史查询筛选功能

### 6. 任务通知显示问题
**问题**: 当前实时查询还会显示"任务#null已进入任务中心"
**修复**: 修复实时查询模式下不应显示任务通知的问题

## 任务中心模块

### 7. 批量建群邀请客户并执行活跃任务优先置顶
**问题**: '批量建群邀请客户并执行活跃' 需要优先置顶
**修复**: 修改 Order 字段为负数以实现置顶

### 8. 客户总数改为客户分类数量自动分配
**问题**: 取消客户总数，由客户分类数量自动分配，在客户分类中不仅显示分组也简略显示数量
**修复**: 修改任务配置和显示逻辑

### 9. 账号邀请风控处理
**问题**: 执行账号邀请过程中遇到风控需要正确处理
**修复内容**:
- Telegram 调用正常返回失败结果：该客户记为失败，继续邀请同一群的下一个客户
- Telegram 调用直接抛出异常，例如账号冻结或会话失效：当前执行账号标记为失败,不影响任务进行,直到正常结束或全部执行账户失效
- FloodWait 单独等待后恢复
- 调用超时:重试3次后,当前执行账号标记为失败,不影响任务进行,直到正常结束或全部执行账户失效
- "并发账号数"需要设计,需要多线程执行

### 10. 任务#23失败分析
**问题**: 简单的逻辑都没生效
**状态**: 完成时间和开始时间相同，任务立即失败，需要添加日志和错误信息
